version: 2

jobs:
  deploy-job:
    steps:
      - add_ssh_keys:
          fingerprints:
            - "CcbaTOvMbSb7k48c+Ne1ehjNdbaxdSuxAu3DELATh/c"
  one:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run: echo "Ciao circleCI"
      - run: mkdir -p my_workspace
      - run: echo "Trying workspaces" > my_workspace/echo-output
      - persist_to_workspace:
          root: my_workspace
          paths:
            - echo-output
  two:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run: echo "Ciao circleCI 2"
      - attach_workspace:
          at: my_workspace
      - run: |
          if [[ $(cat my_workspace/echo-output) == "Trying workspaces" ]]; then
            echo "It worked";
          else
            echo "Nope!"; exit 1
          fi

workflows:
  version: 2
  one_and_two:
    jobs:
      - one
      - two:
          requires:
            - one
